<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.ced">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.CED"
        android:usesCleartextTraffic="true">
        <activity android:name=".activity.JobMbtiActivity"></activity>
        <activity android:name=".activity.JobInterviewActivity" />
        <activity android:name=".activity.JobInterviewListActivity" />
        <activity android:name=".activity.JobInfoListActivity" />
        <activity android:name=".activity.JobInfoActivity" />
        <activity android:name=".activity.JobSelectorActivity" />
        <activity android:name=".activity.UserStopwatchActivity" />
        <activity android:name=".activity.UserRecordActivity" />
        <activity android:name=".activity.UserRecordWriteActivity" />
        <activity android:name=".activity.UserRecordListActivity" />
        <activity android:name=".activity.LoginActivity" />
        <activity android:name=".activity.JoinActivity" />
        <activity android:name=".activity.MainActivity" />
        <activity android:name=".activity.IntroActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>