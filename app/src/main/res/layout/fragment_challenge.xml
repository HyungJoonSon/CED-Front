<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent">

    <androidx.recyclerview.widget.RecyclerView
        android:layout_width="349dp"
        android:layout_height="493dp"
        android:layout_marginStart="10dp"
        android:layout_marginTop="110dp"
        android:layout_marginEnd="10dp"
        android:layout_marginBottom="80dp"
        app:layout_constraintBottom_toTopOf="@+id/rankbotton"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toBottomOf="@+id/ranktop" />

    <LinearLayout
        android:id="@+id/ranktop"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:gravity="center_vertical"
        android:orientation="horizontal"
        android:paddingLeft="15dp"
        android:paddingTop="10dp"
        android:paddingRight="15dp"
        android:paddingBottom="10dp"
        app:layout_constraintBottom_toTopOf="@+id/recyclerView"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent"
        tools:ignore="UnknownId">

        <TextView
            android:layout_width="230dp"
            android:layout_height="wrap_content"
            android:fontFamily="@font/bmhanna"
            android:gravity="center_vertical"
            android:text="CHALLENGE"
            android:textColor="@color/black"
            android:textSize="40dp" />
    </LinearLayout>

    <!-- 밑에 속성 중에서 layout_marginBottom 값을 올리면 위로 올라감
     올려줄 때, _marginBottom밑에 있는 LinearLayout의 layout도
     같은 값으로 올려줘야함(여기서 +10dp를 했으면 밑도 +10dp)-->

    <!-- 밑에 속성 중에서 layout_marginBottom 값을 올리면 위로 올라감
     올려줄 때, 위에 있는 TableLayout의 layout_marginBottom도
     같은 값으로 올려줘야함(여기서 +10dp를 했으면 위도 +10dp)-->

    <LinearLayout
        android:id="@+id/rankbotton"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:gravity="center_vertical"
        android:orientation="horizontal"
        android:paddingLeft="15dp"
        android:paddingTop="10dp"
        android:paddingRight="15dp"
        android:paddingBottom="10dp"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent">

        <Button
            android:layout_width="match_parent"
            android:layout_height="60dp"
            android:background="@drawable/rank_stopwatch_ripple"
            android:text="시작버튼"
            android:textColor="@color/black"/>
    </LinearLayout>

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:orientation="vertical"
        tools:layout_editor_absoluteX="192dp"
        tools:layout_editor_absoluteY="256dp"></LinearLayout>

</androidx.constraintlayout.widget.ConstraintLayout>